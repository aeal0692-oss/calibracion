{
  "name": "calibracion-revisor",
  "version": "1.0.0",
  "description": "Proxy del revisor de Calibracion (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "prueba": "node --experimental-strip-types prueba-presupuesto.mjs"
  },
  "type": "module",
  "private": true,
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260906.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.0"
  }
}
